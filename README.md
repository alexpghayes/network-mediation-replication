# Network mediation via spectral network regression

[netmediate](https://github.com/alexpghayes/netmediate/) R Package


To compare the paper at two different points in commit history:

``` 
latexdiff-vc --git -r 3327b79 -r HEAD --flatten paper.tex 
```

To prepare the paper for submission to arxiv

```
arxiv-collector --verbose paper.tex
```
